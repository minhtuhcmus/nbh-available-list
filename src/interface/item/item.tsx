export interface IItemDetail {
  name: string;
  available: number;
  packaging: number;
  origin: string;
  price: string;
  length: string;
  color: string;
  images: string;
  orderBy: string;
  note: string;
}